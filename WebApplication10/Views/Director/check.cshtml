@model IEnumerable<WebApplication10.Models.projectRequest>

@{
    ViewBag.Title = "check";
    //Layout = "~/views/shared/_directorlayout.cshtml";
    Layout = "~/views/shared/_adminlayout.cshtml";
}

<h2>check Requests</h2>
<table class="table " >
    <tr>
        @*<th>
            @Html.DisplayNameFor(model => model.time)
        </th>*@

        <th>
            @Html.DisplayNameFor(model => model.projects.titel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.projects.id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.status.name_)
        </th>
        <th>
            action
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            @*<td>
                @Html.DisplayFor(modelItem => item.time)
            </td>*@
            @*<td>
                    @Html.DisplayFor(modelItem => item.status.name_)
                 @if(item.status.name_== "accepted")
                 {
                     <td>
                   @Html.ActionLink("Edit", "Edit", new { id = item.Id })
                         </td>
                 }
                </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.projects.titel)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.projects.id)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.status.name_)
                @if (item.status.name_ == "accepted")
                {
                <td>
                    @Html.ActionLink("sechdual project", "ScheduleProject", new { id = item.projects.id }, new { @class = "btn btn-warning" })
                    @Html.ActionLink("invet team members", "inviteTeam", new { proid = item.projects.id }, new { @class = "btn btn-warning" })
                </td>



                }
                else
                {
                    <th></th>
                }
                </td>

            </tr>
    }

</table>









